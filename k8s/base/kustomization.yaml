apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - pdb.yaml

configMapGenerator:
  - name: energy-consumption-config
    literals:
      - SPRING_DATASOURCE_URL=jdbc:postgresql://energy-consumption-db-rw:5432/energy_consumption
