info:
  name: getReadings
  type: http
  seq: 1

http:
  method: GET
  url: "{{backendBaseUrl}}/meters/{{meterId}}/readings"
  auth: inherit

runtime:
  variables:
    - name: meterId
      value: "1"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
