info:
  name: updateReading
  type: http
  seq: 5

http:
  method: PUT
  url: "{{backendBaseUrl}}/api/meters/{{meterId}}/readings/{{id}}"
  body:
    type: json
    data: |-
      {
        "type": "GAS",
        "locationId": 53
      }
  auth: inherit

runtime:
  variables:
    - name: id
      value: "1"
    - name: meterId
      value: "1"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
