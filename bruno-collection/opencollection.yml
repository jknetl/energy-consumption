opencollection: 1.0.0

info:
  name: Energy Consumption
request:
  auth:
    type: bearer
    token: "{{accessToken}}"
bundled: false
extensions:
  bruno:
    ignore:
      - node_modules
      - .git
