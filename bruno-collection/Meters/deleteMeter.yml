info:
  name: deleteMeter
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{backendBaseUrl}}/api/meters/{{id}}"
  auth: inherit

runtime:
  variables:
    - name: id
      value: "1"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
