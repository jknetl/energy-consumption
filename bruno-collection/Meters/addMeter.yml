info:
  name: addMeter
  type: http
  seq: 2

http:
  method: POST
  url: "{{backendBaseUrl}}/meters"
  body:
    type: json
    data: |-
      {
        "type": "GAS",
        "locationId": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
