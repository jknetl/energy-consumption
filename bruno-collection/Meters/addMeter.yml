info:
  name: addMeter
  type: http
  seq: 2

http:
  method: POST
  url: "{{backendBaseUrl}}/api/meters"
  body:
    type: json
    data: |-
      {
        "type": "GAS",
        "locationId": 1
      }
  auth:
    mode: bearer
    bearer:
      token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
