info:
  name: addMeter
  type: http
  seq: 1

http:
  method: POST
  url: "{{backendBaseUrl}}/api/meters"
  body:
    type: json
    data: |-
      {
        "type": "ELECTRICITY",
        "locationId": 52
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
