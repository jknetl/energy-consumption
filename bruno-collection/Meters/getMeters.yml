info:
  name: getMeters
  type: http
  seq: 2

http:
  method: GET
  url: "{{backendBaseUrl}}/api/meters"
  auth:
    mode: bearer
    bearer:
      token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
