info:
  name: getMeters
  type: http
  seq: 2

http:
  method: GET
  url: "{{backendBaseUrl}}/meters"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
