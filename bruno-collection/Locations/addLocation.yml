info:
  name: addLocation
  type: http
  seq: 2

http:
  method: POST
  url: "{{backendBaseUrl}}/api/locations"
  body:
    type: json
    data: |2-
        {
          "id": 52,
          "street": "Main Street",
          "streetNumber": 1,
          "postalCode": 11000,
          "city": "Prague",
          "countryCode": "CZE"
        }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
