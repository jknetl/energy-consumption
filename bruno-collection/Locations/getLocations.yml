info:
  name: getLocations
  type: http
  seq: 1

http:
  method: GET
  url: "{{backendBaseUrl}}/api/locations"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
