spring:
  application:
    name: energy-consumption
  jpa:
    hibernate:
      ddl-auto: validate

management:
  endpoint:
    health:
      probes:
        enabled: true
  endpoints:
    web:
      exposure:
        include: health
