spring:
  application:
    name: energy-consumption
  jpa:
    hibernate:
      ddl-auto: validate

management:
  server:
    port: 8081
  endpoint:
    health:
      probes:
        enabled: true
  endpoints:
    web:
      exposure:
        include: health
