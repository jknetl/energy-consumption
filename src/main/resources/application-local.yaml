spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/energy_consumption
    username: ${PGUSER}
    password: ${PGPASSWORD}
  jpa:
    hibernate:
      ddl-auto: create-drop

app:
  init:
    user-a-password: password-a
    user-b-password: password-b